#include <iostream>
using namespace std;
int main()
{
	double Y = 1;
	double n = 0;
	cout << "Vvedite n->";
	cin >> n;
	for (size_t i = 0; i < n; i++)
	{
		if (floor(n) == n && n < 10)
		{
			Y += (1) / (pow(i, 5));
		}
	}
	for (size_t i = 0; i < 10; i++)
	{
		Y *= (2 + 1 / );
	}

}